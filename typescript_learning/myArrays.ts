let superHeros: string[] = [];
let heroPower: Array<number> = [];

type User = {
    name: string;
    isActive: boolean
}

const allUsers: User[] = []

const MLModels: number[][] = [
    [255, 255, 255],
    [0, 0]
]

superHeros.push("spiderMan");
heroPower.push(2);
allUsers.push({ name: "Bruce", isActive: true }, { name: "Clark", isActive: false });